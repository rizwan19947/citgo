"use client";

/*
 * Site footer component. Marked "use client" so it can hold interactive elements
 * (e.g. newsletter signup, analytics events). Currently a placeholder — add your
 * footer links, copyright, and social icons here.
 */
export default function Footer() {
  return (
    <footer className="flex justify-center items-center bg-gray-400">
      <p>Footer</p>
    </footer>
  );
}
